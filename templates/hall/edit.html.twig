{% extends 'base.html.twig' %}

{% block title %}Edit Hall{% endblock %}

{% block main %}
    <h1>Edit Hall</h1>

    {{ include('hall/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_hall_index') }}">back to list</a>

    {{ include('hall/_delete_form.html.twig') }}
{% endblock %}
