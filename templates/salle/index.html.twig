{% extends 'base.html.twig' %}

{% block title %}salle!{% endblock %}

{% block body %}
<h1>Todo index</h1>


<table class="table" border="1">
<thead>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th>details</th>
            
            
            
        </tr>
    </thead>
    <tbody>
  {% for salle in salles %}
  
 
     <tr>
            <td>{{ salle.id }}</td>
            <td>{{ salle.nomsalle }}</td>
            <td><a href="{{ path('show', {'id': salle.id}) }}">show</a></td>
           </tr>

       
  {% endfor %} {# salles #}

    </tbody>
</table>

{% endblock %} {# body #}